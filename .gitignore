.DS_Store
work_dir/

# CV3 production deployment - secrets and rendered (host-specific) config
production/.env
production/config/*.json
production/secrets/
production/backups/
*.unseal

# Real (host-specific) ansible inventory - keep locally next to the committed
# *.example files for manual A-Z passes; never commit (contains IP + credentials).
production/inventory/inventory
production/inventory/host_vars/cafe-variome-node/vars.yml
